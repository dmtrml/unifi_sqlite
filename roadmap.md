# Roadmap

## 2024-12-15

### ✅ Сделано
- Перенос фильтров и отчётов на иерархию категорий: добавлены `parentId`, формы выбора родителя, группировка и отображение детей в `CategoryBreakdownChart` и `CategorySpendingChart`.
- Новый отчётный эндпоинт `/api/reports/transactions` без лимита, SWR-фетч с DateRangePicker, модальные таблицы транзакций по клику на чарты.
- Упорядочивание UI: фильтры в шите, адаптивный DateRangePicker, переработанный список транзакций (иконки категорий, тип суммы, группировка по дням).
- Отчёты и графики: подсегменты подкатегорий в прогресс-барах и тултипах, модалка транзакций учитывает всех потомков и `uncategorized`.
- Категории: страница группирует по типу, аккордеоны + иконки детей, добавлен `roadmap` трекер задач (текущий файл).
- Fixes: клики по «Other» на донат-чарте отключены, «Uncategorized» фильтруется как `category_id IS NULL` + `transaction_type = expense`.

### 🔜 Следующие шаги
1. **Доработать Dashboard**
   - Подключить DateRangePicker/фильтры как на отчётах, чтобы KPI и графики реагировали на выбор периода.
   - Переписать карточки транзакций/спендинга на переиспользуемых компонентах (иконки категорий, цвета, дополнительные метрики).
   - Добавить секцию «быстрые действия» (ссылки на импорт, бюджеты, отчёты) и, при возможности, выводить alert’ы по превышенным бюджетам.

2. **Импорт данных**
   - ✅ Сделать пресеты маппинга для популярных приложений/банков (CSV-шаблоны, сохранённые правила). Сейчас доступны ZenMoney и Monefy, каждый со своими полями, автоматическим сопоставлением и обработкой переводов.
   - В мастер импорта добавить шаг формирования иерархии категорий: предложить пользователю соответствия, автоматически создавать родителей.
   - Перепроверить расчёт балансов после импорта (пересчёт сумм по счетам, защита от дублей трансферов).

3. **Transactions Page**
   - Расширить фильтры: добавление «последние N дней», «только uncategorized», мультиселект по категориям и тегам.
   - Обновить карточки (десктоп/мобайл): компактные строки с иконками, action-баттонами и подсказками по повторяющимся платежам.
   - Реализовать бесконечную пагинацию и кнопку «быстрый экспорт» для текущей выборки.

4. **Accounts Page**
   - Добавить график тренда баланса (Sparkline или линейный график за последние 3/6 месяцев).
   - Показать «балансовый пончик»: распределение по валютам и типам счетов.
   - В аналитике счета давать breakdown по категориям (аналитика + фильтр по периоду).

5. **Categories Page**
   - Улучшить визуал: карточки/таблицы с цветными иконками, счётчиками транзакций, статусами (например, привязаны ли бюджеты).
   - Добавить быстрые действия: создание подкатегории, переименование, перенос ветки, массовое назначение транзакций.

6. **Budgets Page**
   - Продумать UI: набор карт «бюджет по категории», прогресс-бары, индикатор «осталось X%».
   - Добавить возможность задавать период действия (месяц/квартал) и автоперенос остатка.
   - Интегрировать напоминания/уведомления и отображение превышений на Dashboard.

7. **Recurring Page**
   - Привести к единому мастеру (создание/редактирование), выводить календарь ближайших списаний.
   - Добавить статусы (активный, приостановлен, ошибка), дублирование расписаний и синхронизацию с транзакциями.

8. **Reports**
   - Исправить Income vs. Expense: на месячном диапазоне показывать реальные данные (не усреднять, правильно группировать).
   - Добавить сравнение с предыдущим периодом и возможность экспортировать графики в CSV/PNG.
